import { styled } from "linaria/react";

export const TradingButtonWrapper = styled.div`
    margin: 1rem 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(9rem, 1fr));
    grid-gap: 1rem;
`;
